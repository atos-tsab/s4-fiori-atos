<mvc:View 
    controllerName="z.delivery.controller.Delivery"
    xmlns:mvc="sap.ui.core.mvc" 
	xmlns:core="sap.ui.core"
	displayBlock="true"
    xmlns="sap.m">

	<Page id="page" title="{i18n>title}" showNavButton="false" navButtonPress="onNavBack" navButtonTooltip="{i18n>Back}">
 		<customHeader>
			<Toolbar>
				<Title text="{ScanModel>/viewTitle}" titleStyle="H5" class="sapUiSmallMarginBegin" />
				<ToolbarSpacer />
					<Button id="idButtonSave_DELIV" press="onPressSave" text="{i18n>SaveF5}" type="Reject" width="100px" enabled="{ScanModel>/saving}" />
					<ToolbarSeparator />
					<Button id="idButtonBook_DELIV" press="onPressBooking" text="{i18n>Book}" type="Reject" width="100px" enabled="{ScanModel>/booking}" />
					<ToolbarSeparator />
					<Button press="onPressOk(${ScanModel>/viewMode})" text="{i18n>OkF2}" type="Success" width="100px" enabled="{ScanModel>/ok}" />
					<ToolbarSeparator />
					<Button press="onPressRefresh" text="{i18n>Refresh}" type="Emphasized" width="100px" enabled="{ScanModel>/refresh}" class="sapUiSmallMarginEnd" />
			</Toolbar>
		</customHeader>

        <content>
            <core:Fragment fragmentName="z.delivery.view.ScanDetails" type="XML" />
        </content>

		<footer>
			<Toolbar>
				<Button text="{i18n>Back}" type="Back" visible="true" press="onNavBack" />
				<ToolbarSpacer />
			</Toolbar>
		</footer>
    </Page>

</mvc:View>