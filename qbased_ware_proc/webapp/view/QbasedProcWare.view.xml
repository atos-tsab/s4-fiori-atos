<mvc:View 
    controllerName="z.qbasedwareproc.controller.QbasedProcWare"
    xmlns:mvc="sap.ui.core.mvc" 
	xmlns:core="sap.ui.core"
	displayBlock="true"
    xmlns="sap.m">

	<Page id="page" title="{i18n>title}" showNavButton="false" navButtonPress="onNavBack" navButtonTooltip="{i18n>Back}">
 		<customHeader>
			<Toolbar>
				<Title text="{i18n>title}" titleStyle="H5" class="sapUiSmallMarginBegin" />
				<ToolbarSpacer />
					<Button press="onPressMainOk(${ScanModel>/viewMode})" text="{i18n>OkF2}" type="Success" width="100px" enabled="{ScanModel>/ok}" />
					<ToolbarSeparator visible="false" />
					<Button press="onPressRefresh" text="{i18n>Refresh}" type="Emphasized" width="100px" visible="false" enabled="{ScanModel>/refresh}" class="sapUiSmallMarginEnd" />
			</Toolbar>
		</customHeader>

        <content>
            <core:Fragment fragmentName="z.qbasedwareproc.view.parts.OueueSelection" type="XML" />
 		</content>

		<footer>
			<Toolbar>
				<Button text="{i18n>Back}" type="Back" visible="true" press="onNavBack" />
				<ToolbarSpacer />
			</Toolbar>
		</footer>
    </Page>

</mvc:View>