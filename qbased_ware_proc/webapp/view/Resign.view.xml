<mvc:View 
    controllerName="z.qbasedwareproc.controller.Resign"
    xmlns:mvc="sap.ui.core.mvc" 
	xmlns:core="sap.ui.core"
	displayBlock="true"
    xmlns="sap.m">

	<Page id="page" title="{i18n>title}" showNavButton="false" navButtonPress="onNavBack" navButtonTooltip="{i18n>Back}">
 		<customHeader>
			<Toolbar>
				<Title text="{i18n>title}" titleStyle="H5" class="sapUiSmallMarginBegin" />
				<ToolbarSpacer />
					<Button id="idButtonBookHU" press="onPressBooking" text="{i18n>Book}" type="Reject" width="100px" enabled="{ScanModel>/booking}" />
					<ToolbarSeparator />
					<Button press="onPressResignOk(${ScanModel>/viewMode})" text="{i18n>OkF2}" type="Success" width="100px" enabled="{ScanModel>/ok}" />
					<ToolbarSeparator />
					<Button press="onPressRefresh" text="{i18n>Clear}" type="Emphasized" width="100px" enabled="{ScanModel>/refresh}" class="sapUiSmallMarginEnd" />
			</Toolbar>
		</customHeader>

        <content>
            <core:Fragment fragmentName="z.qbasedwareproc.view.parts.ResignSelection" type="XML" />
 		</content>

		<footer>
			<Toolbar>
				<Button text="{i18n>Back}" type="Back" visible="true" press="onNavBack()" />
				<ToolbarSpacer />
			</Toolbar>
		</footer>
    </Page>

</mvc:View>